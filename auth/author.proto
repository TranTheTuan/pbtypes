syntax = "proto3";

package auth;

option go_package = "github.com/TranTheTuan/auth/v1";

service AuthorizeService {
  rpc Authorize(AuthorizeRequest) returns (AuthorizeResponse);
}

message AuthorizeRequest {
  string title = 1;
}

message AuthorizeResponse {
  string id = 1;
}
